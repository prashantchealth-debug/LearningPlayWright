//let x = 10;
//console.log(x); // Output: 10

console.log(process.platform); // Output: 'win32' (or 'darwin', 'linux', etc. depending on the operating system)

console.log(process.arch); // Output: 'x64' (or 'arm64', etc. depending on the architecture)

console.log("Node Version", process.version); // Output: 'v14.17.0' (or the version of Node.js you are using)